package com.saudi.tourism.core.models.components.tripplan.v1;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertTrue;

/**
 * Some Unit Tests for TripPoint class.
 */
class TripPointTest {

  // TODO: Fake test added for exclude this test from SonarCube issues
  // Remove it after adding a real test cases
  @Test
  void fakeTest() {
    assertTrue(true);
  }

  // TODO FIXME Please fix this unit test
//  @Test
//  void copyConstructorTest() throws CloneNotSupportedException {
//    final EventDetail someEvent = mock(EventDetail.class);
//
//    final TripPoint fullEventPoint = new TripPoint(someEvent);
//    assertEquals(TripPlanConstants.TYPE_EVENT, fullEventPoint.getType());
//    assertEquals(someEvent, fullEventPoint.getEvent());
//    assertEquals(someEvent.getId(), fullEventPoint.getId());
//    assertNotNull(fullEventPoint.getEvent());
//  }
}
