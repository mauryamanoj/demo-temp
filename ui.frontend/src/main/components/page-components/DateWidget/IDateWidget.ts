export interface DateWidgetProps {
  title: string;
  startDate: string;
  endDate: string;
  comingSoonLabel: string;
  expiredLabel: string;
  componentId: string;
}

export interface DateTextProps {
  text: string;
  styles: string;
}
